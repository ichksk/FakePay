import { Background } from "./background"
import { Settlement } from "./settlement"

export const ResultView = () => {
    return (
        <Background>
            <Settlement/>
        </Background>
    )
}